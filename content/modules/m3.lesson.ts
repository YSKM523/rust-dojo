export const m3Lesson = [
  '## trait、泛型与生命周期',
  '',
  '这一模块是从“能写几个 Rust 函数”走向“能设计 Rust API、能读懂生态文档”的分水岭。你会把不同类型的共同能力抽成 trait，用泛型在不牺牲类型安全的前提下复用算法，再用生命周期准确描述引用之间的来源关系。',
  '`tokio`、`axum`、`serde` 的文档里经常出现 `T: Send + Sync + \'static`、`impl IntoResponse`、`Box<dyn Error>`。这些符号不是炫技，而是在公开 API 上写清楚“调用者必须提供什么能力、返回值可能是什么、引用能用多久”。掌握它们之后，复杂签名会从一堵墙变成一张可读的契约。',
  '',
  '---',
  '',
  '### 一、trait 是能力契约',
  '',
  'trait 定义一组共享行为。它不关心类型有哪些字段，只关心类型能否兑现方法签名：',
  '',
  '```rust',
  'trait Summary {',
  '    fn title(&self) -> &str;',
  '',
  '    fn summarize(&self) -> String {',
  '        format!("（继续阅读）{}", self.title())',
  '    }',
  '}',
  '',
  'struct Article { title: String }',
  '',
  'impl Summary for Article {',
  '    fn title(&self) -> &str { &self.title }',
  '}',
  '```',
  '',
  '`title` 只有签名、没有方法体，是 required 方法；实现者必须提供它。`summarize` 有方法体，是默认方法，实现者可以直接继承。默认方法还能调用同一 trait 的 required 方法，因此可以把稳定的流程集中在契约里，把类型差异留给具体实现。',
  '',
  '它和 Java、TypeScript 的 interface 都是在描述能力，但 Rust 的实现是静态检查的，而且可以为既有类型补充行为。它也不同于 Python、Go 常被提到的 duck typing：duck typing 倾向于“只要运行时看起来有这个方法就调用”，Rust 则要求 `impl Summary for Article` 明确成立，错误会在编译期暴露。Go 的接口满足通常是隐式的，Rust 的 trait 实现是显式的，读代码时更容易追踪能力来自哪里。',
  '',
  'Rust 还有一致性规则：实现 trait 时，trait 或目标类型至少有一个定义在当前 crate。你可以为自己的 `Article` 实现标准库的 `Display`，也可以为标准库类型实现自己的 trait，但不能在自己的 crate 里为 `String` 实现标准库的 `Display`。这条“孤儿规则”避免两个依赖偷偷提供冲突实现。',
  '',
  '### 二、默认方法与覆盖',
  '',
  '默认方法适合表达大多数实现都共享的行为；特殊类型可以写同名方法覆盖它：',
  '',
  '```rust',
  'struct Alert { level: u8, title: String }',
  '',
  'impl Summary for Alert {',
  '    fn title(&self) -> &str { &self.title }',
  '',
  '    fn summarize(&self) -> String {',
  '        format!("[P{}] {}", self.level, self.title())',
  '    }',
  '}',
  '```',
  '',
  '覆盖不是继承体系里的虚方法重写：类型没有父子关系，只是选择如何兑现同一份能力契约。设计 trait 时应优先放稳定、最小的 required 方法，再由默认方法组合出便利行为。required 方法太多会让实现成本变高；默认方法偷偷假设某个具体类型的字段，又会破坏抽象边界。',
  '',
  '调用 trait 方法时，trait 还必须在作用域中。看到“method not found”时，不只要检查类型是否实现了 trait，也要检查是否漏了 `use some_crate::Summary;`。这是使用第三方扩展 trait 时很常见的报错来源。',
  '',
  '### 三、泛型不是“关闭类型检查”',
  '',
  '泛型让一份定义适用于一组类型：',
  '',
  '```rust',
  'struct Pair<T> {',
  '    x: T,',
  '    y: T,',
  '}',
  '',
  'fn first<T>(pair: &Pair<T>) -> &T {',
  '    &pair.x',
  '}',
  '```',
  '',
  '`T` 是调用时才确定的类型参数，但函数体仍然要对所有允许的 `T` 都成立。上面的代码只借用字段，不要求额外能力；如果要比较、打印或克隆，就必须用 trait bound 把能力写进签名。泛型不是动态语言里的“随便传”，而是“对满足契约的所有类型都正确”。',
  '',
  '约束还可以只放在某个 `impl` 上。`Pair<T>` 本身能保存任何两个同类型的值，但比较并打印的方法只有在能力足够时才出现：',
  '',
  '```rust',
  'use std::fmt::Display;',
  '',
  'impl<T: Display + PartialOrd> Pair<T> {',
  '    fn cmp_display(&self) {',
  '        if self.x >= self.y {',
  '            println!("x = {}", self.x);',
  '        } else {',
  '            println!("y = {}", self.y);',
  '        }',
  '    }',
  '}',
  '```',
  '',
  '这种条件实现比给整个结构体施加约束更灵活：不能比较的类型依旧能构造 `Pair`，只是不能调用 `cmp_display`。',
  '',
  '### 四、trait bound 的三种常用写法',
  '',
  '第一种写在泛型参数旁，适合约束短、参数少的函数：',
  '',
  '```rust',
  'fn largest<T: PartialOrd + Copy>(list: &[T]) -> T {',
  '    // ...',
  '    list[0]',
  '}',
  '```',
  '',
  '第二种是 `where` 子句，适合约束较多或涉及多个类型时。它通常最利于阅读：',
  '',
  '```rust',
  'fn notify<T, U>(item: &T, logger: &U)',
  'where',
  '    T: Summary + Display,',
  '    U: Logger + ?Sized,',
  '{',
  '    // ...',
  '}',
  '```',
  '',
  '第三种是 `impl Trait`。参数位置的 `impl Summary` 是简洁语法，表示“接受某个实现了 Summary 的具体类型”；当调用者需要显式指定类型、多个参数必须是同一个具体类型，或签名需要复用类型参数时，仍应写 `<T: Summary>`。',
  '',
  '```rust',
  'fn print_summary(item: &impl Summary) {',
  '    println!("{}", item.summarize());',
  '}',
  '',
  'fn make_summary() -> impl Summary {',
  '    Article { title: String::from("Rust") }',
  '}',
  '```',
  '',
  '返回位置的 `impl Trait` 会隐藏具体返回类型，但函数内部仍必须始终返回同一个具体类型。不能在一个分支返回 `Article`、另一个分支返回 `Alert`，即使两者都实现了 `Summary`；需要混合类型时要考虑枚举或 trait object。',
  '',
  '### 五、静态分发：泛型为什么通常没有运行时成本',
  '',
  '泛型和 `impl Trait` 通常采用静态分发。编译器会做单态化：发现 `largest::<i32>` 和 `largest::<char>` 两种实际调用后，为它们生成针对具体类型的机器码。调用目标在编译期已知，容易内联和优化，不需要运行时查询类型。',
  '',
  '代价是编译器可能生成多份代码，增加编译时间和二进制体积。大多数业务代码应先选择泛型，因为它类型信息完整、性能可预测；只有当异构存储、插件边界或减少泛型传播确实重要时，再引入动态分发。',
  '',
  '### 六、动态分发：dyn Trait 与 vtable',
  '',
  '下面的集合要同时保存圆和矩形，元素没有同一个具体类型：',
  '',
  '```rust',
  'trait Shape {',
  '    fn area(&self) -> f64;',
  '}',
  '',
  'let shapes: Vec<Box<dyn Shape>> = vec![',
  '    Box::new(Circle { radius: 2.0 }),',
  '    Box::new(Rectangle { width: 3.0, height: 4.0 }),',
  '];',
  '',
  'for shape in &shapes {',
  '    println!("{:.2}", shape.area());',
  '}',
  '```',
  '',
  '`dyn Shape` 是 trait object，常见形式有 `&dyn Shape`、`Box<dyn Shape>`、`Arc<dyn Shape + Send + Sync>`。它是一个胖指针：一部分指向具体数据，另一部分指向 vtable。vtable 记录该具体类型的方法入口和析构等信息，调用 `area` 时才通过表找到目标函数，这就是动态分发。',
  '',
  '动态分发允许一个集合容纳不同具体类型，也能隐藏实现细节、缩小公开 API 的泛型扩散。成本包括一次间接调用、较少的内联机会，以及必须满足对象安全规则。带泛型参数的方法、返回裸 `Self` 的方法通常不能直接通过 trait object 调用；编译器会用“trait is not dyn compatible”一类诊断指出问题。',
  '',
  '面试高频取舍可以这样回答：类型集合在编译期固定、追求性能和类型信息时用泛型；运行时才决定实现、需要异构集合或插件式扩展时用 `dyn`。`Box` 不是动态分发本身，它只是给大小未知的 `dyn Trait` 提供固定大小的拥有型指针；借用场景用 `&dyn Trait` 即可。',
  '',
  '### 七、生命周期标注的是关系，不是时长',
  '',
  '生命周期最重要的直觉是：它不会延长任何值的存活时间，也不会控制何时 drop。它只是把引用之间的关系标给借用检查器看。',
  '',
  '```rust',
  "fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {",
  '    if x.len() >= y.len() { x } else { y }',
  '}',
  '```',
  '',
  '这里的 `\'a` 表示：返回引用来自输入，且返回值可用的范围不能超过 `x` 与 `y` 都有效的重叠范围。它不是说两个输入必须活得一样久，也不是命令它们活到某个指定时刻。调用点会根据实际借用范围推导一个满足约束的最短关系。',
  '',
  '为什么返回值要和两个输入写同一个 `\'a`？函数可能返回任意一侧，编译器必须采用两侧都安全的范围。若业务上永远返回 `x`，签名可以只把输出和 `x` 关联，`y` 使用另一个生命周期；标注应忠实表达真实数据流，而不是越长越好。',
  '',
  '### 八、生命周期省略规则三条',
  '',
  '多数引用签名不用手写生命周期，是因为编译器按三条确定规则补全，而不是因为生命周期不存在：',
  '',
  '1. 每个省略的输入引用都获得自己的生命周期参数。例如 `fn f(x: &str, y: &str)` 会得到两个不同的输入生命周期。',
  '2. 如果恰好只有一个输入生命周期，它会被赋给所有省略的输出引用。例如 `fn first(x: &str) -> &str` 的输出显然借自 `x`。',
  '3. 如果有多个输入生命周期，但其中一个是方法接收者 `&self` 或 `&mut self`，那么 `self` 的生命周期会赋给所有省略的输出引用。',
  '',
  '三条规则走完仍无法唯一确定输出来源时，就会出现 `E0106`，要求你显式标注。`longest(x: &str, y: &str) -> &str` 有两个候选输入又没有 `self`，编译器不会猜。生命周期标注只出现在签名中，通常不需要把函数体里的每个引用都标出来。',
  '',
  '### 九、`\'static` 的两种含义',
  '',
  '第一种是引用生命周期：`&\'static str` 表示引用指向的数据在整个程序运行期间都有效。字符串字面量被放进程序二进制，因此 `let name: &\'static str = "dojo";` 合法。普通局部 `String` 的切片不是静态引用，不能靠写标注把它变成静态。',
  '',
  '第二种是 trait bound：`T: \'static` 表示 `T` 不能包含生命周期短于静态期的借用数据。它不等于“这个值永远不会释放”；拥有数据的 `String` 满足 `\'static` bound，但变量照样会在离开作用域时 drop。`tokio::spawn` 常要求 Future 是 `\'static`，意思是任务不能借用可能提前消失的栈数据，通常要用 `move` 把所有权带进任务。',
  '',
  '不要把 `Box::leak` 当作修复生命周期报错的常规工具。它会故意泄漏内存来得到静态引用，只适合极少数进程级只初始化一次的数据。大多数 E0106 或“does not live long enough”都应该通过调整所有权、缩短借用或准确描述关系解决。',
  '',
  '### 十、struct 持有引用',
  '',
  '结构体一旦保存引用，就必须保证实例不能比被借用数据活得更久：',
  '',
  '```rust',
  "struct Excerpt<'a> {",
  "    part: &'a str,",
  '}',
  '',
  "impl<'a> Excerpt<'a> {",
  '    fn part(&self) -> &str {',
  '        self.part',
  '    }',
  '}',
  '```',
  '',
  '`Excerpt<\'a>` 的含义是“这个值内部借用了至少在 `\'a` 期间有效的文本”。创建实例时，编译器会从实际引用推导 `\'a`；如果原字符串先离开作用域而结构体之后仍被使用，编译就会失败。',
  '',
  '在领域模型中大量保存引用会让生命周期沿类型层层传播。若数据很小、需要跨线程或长期缓存，拥有型字段 `String` 往往更简单；若对象只是临时解析视图、追求零拷贝，引用字段就很合适。选择标准不是“引用一定更快”，而是所有权边界是否清晰。',
  '',
  '### 十一、读懂 E0369、E0106 与 E0515',
  '',
  '**E0369：某个运算不能用于泛型类型。** 看到 `binary operation > cannot be applied to type T`，先问这个操作由哪个 trait 提供。比较通常需要 `PartialOrd`，相等需要 `PartialEq`，格式化 `{}` 需要 `Display`。把最小必要 bound 加到函数或条件 `impl`，不要图省事把类型写死。',
  '',
  '**E0106：缺少生命周期说明。** 常见于多个输入引用对应一个输出引用，或 struct 字段保存引用。先判断输出到底借自谁，再用同一个生命周期名连接真实来源；结构体则要在类型和 `impl` 上都声明参数。它要求的是关系，不是让你随手加 `\'static`。',
  '',
  '**E0515：返回了局部数据的引用。** `fn make() -> &str { let s = String::from("x"); &s }` 无法修成安全引用，因为 `s` 会在函数结束时被释放。正确方案通常是返回 `String` 所有权，或让函数接收外部引用并返回其中的切片。生命周期参数不能复活已经 drop 的值。',
  '',
  '处理编译错误时按顺序读：先看主错误码和一句话结论，再看标出“borrowed here”“returned here”的位置，最后读 `help` 建议。一次只修最前面的根因；后续错误经常只是同一问题的连锁反应。',
  '',
  '### 十二、和真实后端工作、面试的连接',
  '',
  '读 `serde` 时，你会看到 `Serialize`、`Deserialize<\'de>` 以及带条件的派生实现；`\'de` 正是在描述反序列化结果可能借用输入字节。读 `axum` 时，handler 参数通过各种 trait 转成提取器，返回值通过 `IntoResponse` 统一成响应。读 `tokio` 时，`Send + \'static` 决定一个异步任务能否安全地在线程间调度并独立存活。',
  '',
  'trait objects vs 泛型几乎是 Rust 面试标配题。合格回答不只是“一个静态、一个动态”，还应说出单态化、vtable、异构集合、内联机会、二进制体积和对象安全。生命周期题也不应背成“防止悬垂引用”一句话，而要能指出签名中输出引用与哪个输入相关，以及为什么返回局部引用永远不成立。',
  '',
  '在 API 设计里，优先用 trait 表达最小能力，用 `where` 让复杂约束可读，用条件实现避免过度限制类型；在需要异构存储时使用 `dyn`；只有引用关系无法由省略规则确定时才显式写生命周期。这样写出的接口既精确，又不会把调用者困进无意义的类型体操。',
  '',
  '---',
  '',
  '### 完成本模块后你能…',
  '',
  '- [ ] 把 trait 解释为显式、编译期检查的能力契约，并说清它与 interface、duck typing 的异同',
  '- [ ] 定义 required 方法、默认方法，并为特殊类型 override 默认行为',
  '- [ ] 写出泛型函数、泛型结构体与条件 `impl`，根据操作补上最小 trait bound',
  '- [ ] 熟练辨认 `<T: Trait>`、`where`、参数与返回位置的 `impl Trait`',
  '- [ ] 比较静态分发与动态分发，说清单态化、胖指针、vtable 和 `dyn` 的使用场景',
  '- [ ] 把生命周期理解为引用关系标注，准确复述三条省略规则与 `\'static` 的两种含义',
  '- [ ] 为持有引用的 struct 和对应 `impl` 写出生命周期参数',
  '- [ ] 根据 E0369、E0106、E0515 找到缺失约束、模糊来源或悬垂引用，并选择所有权或借用方案修复',
  '- [ ] 读懂 tokio、axum、serde 文档中的常见 trait、泛型与生命周期签名，并能回答相关面试题',
].join('\n');
